class Day {
  constructor(low, high, precipitation, sunrise, sunset, description, aqi) {
    this.low = low;
    this.high = high;
    this.precipitation = precipitation;
    this.sunrise = sunrise;
    this.sunset = sunset;
    this.description = description;
    this.aqi = aqi;
  }
}
